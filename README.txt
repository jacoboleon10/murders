We analyze US gun murder rate data collected by the FBI

download-data.R - download csv file to data directory

wrangle-data.R - creates a derived dataset and saves it as an R object in the rda directory

analysis.R - generates a plot and saves it to figs directory